---
name: Preview Documentation

"on":
  pull_request:
    types:
      - "opened"
      - "reopened"
      - "synchronize"
      - "closed"
    # Keep in step with docs.yaml: this is the only strict build a PR gets.
    paths:
      - "docs/**"
      - "overrides/**"
      - "zensical.toml"
      # The build runs from the lockfile, so a zensical bump is a change to
      # the site.
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/preview.yaml"

permissions:
  contents: read

concurrency:
  group: "pr-preview-${{ github.event.number }}"
  cancel-in-progress: true

jobs:
  preview:
    # Fork PRs have no write access, so the deploy would fail.
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Install uv
        if: github.event.action != 'closed'
        uses: astral-sh/setup-uv@c18668ad3cf93ea998bef934396af7bb5c839dc7 # v10.2.0
        with:
          version: "0.12.17"

      - name: Build site
        if: github.event.action != 'closed'
        run: |
          uv python install
          uv sync --locked --all-extras --dev
          uv run zensical build --clean --strict

      # On "closed" this removes the preview instead of publishing one.
      - name: Deploy preview
        uses: rossjrw/pr-preview-action@ffa7509e91a3ec8dfc2e5536c4d5c1acdf7a6de9 # v1.8.1
        with:
          source-dir: site
          preview-branch: gh-pages
          umbrella-dir: pr-preview
          action: auto
