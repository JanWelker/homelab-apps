[project]
name = "homelab-apps"
version = "0.1.0"
description = "Workloads running on the Flatcar homelab cluster"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "zensical>=0.0.59",
    "yamllint>=1.37.1",
    "pre-commit>=4.5.1",
    "pyyaml>=6.0.3",
]
